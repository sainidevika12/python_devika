```py
#Using a For loop in Python, print your name 10 times

#Using a For loop in Python, print all even numbers between 0 and 101

# #Do the same to print odd numbers between 0 and 101
# Write a function in python that takes length and width of a rectangle as input parameters 
# and returns area of the rectangle.

# Write a function that takes the height in centimeters as input parameter and tells us if
# the person is tall enough to go on a ride in Disney Land

def printingLoop(assignedTask):
 print(" ")
 print(assignedTask)

for printingLoop in nameGiven:
nameGiven="DEVIKA" * 10
printingName("MY NAME IS{}".format(nameGiven))

for printingLoop in range(1,101):
  evenNum=num % 2 == 0:
  printingLoop("the even numbers from 1-101 are{}".format(evenNum))
  if evenNum=num % 1==0:
    printingLoop=evenNum.pop
    print("cannot add odd number in even number list")
printingLoop(evenNum)


for printingLoop in range(1,101):
  evenNum=num % 1 == 0:
  printingLoop("the odd numbers from 1-101 are{}".format(evenNum))
  if evenNum=num % 2==0:
    printingLoop=evenNum.pop
    print("cannot add even number in odd number list")
printingLoop(evenNum)

length=12
width=14

#calculate area of reactangle
areaOfRectangle=length * width
print("the area of rectangle is")
print(areaOfRectangle)

#calculate perimeter of reactangle
perimeter_Rectangle=2 *(length + width)
print("the perimeter of rectangle is")
print(perimeter_Rectangle)



print("enter your height")
given_Feet=5
given_Inches=6

height_In_Feet = int(input(" Number of feet: "))
height_In_Inches = int(input(" Number of inches: "))

height_In_centimetre=(height_In_Feet *given_Feet +height_In_Inches) * given_Inches

print("the height in centimetre is {}".format(height_In_centimetre))


if height_In_centimetre is >= 160:
  print("hooray!you can take this ride")
  else:
  print("oops,you cannot take this ride")
```
